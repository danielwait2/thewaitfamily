import React from "react";

const Footer = () => (
  <footer className="site-footer">
    <div className="footer-inner">
      <p>&copy; {new Date().getFullYear()} The Wait Family. Made with love.</p>
    </div>
  </footer>
);

export default Footer;
